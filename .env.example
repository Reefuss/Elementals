# Copy to .env.local for local development
# Port the unified server (Next.js + Socket.IO) listens on
PORT=3000

# In dev mode, Next.js runs on 3000 and the socket server on 3001
# NEXT_PUBLIC_SOCKET_URL is what the browser uses to connect to Socket.IO
NEXT_PUBLIC_SOCKET_URL=http://localhost:3001
